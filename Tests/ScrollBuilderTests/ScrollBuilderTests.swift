    import XCTest
    @testable import ScrollBuilder

    final class ScrollBuilderTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ScrollBuilder().text, "Hello, World!")
        }
    }
